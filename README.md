# test_project

My first Python project and GitHub setup test.

## What it does
This project runs a simple Python script that prints:

`Hello Physics World`

## Files
- `main.py` – main Python script
- `.gitignore` – ignores virtual environment and cache files

## Run
Activate the virtual environment and run:

```bash
python main.py